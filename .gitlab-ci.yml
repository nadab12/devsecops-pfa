stages:
  - build
  - sast

build-image:
  stage: build
  image: docker:24.0
  services:
    - docker:24.0-dind
  script:
    - docker build -t juice-shop:$CI_COMMIT_SHORT_SHA .

semgrep-scan:
  stage: sast
  image: semgrep/semgrep
  script:
    - semgrep scan --config auto --error